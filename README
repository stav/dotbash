README

Bash dot files used for interactive user on Ubuntu

.bashrc

	Just source .bash_profile

.bash_profile

	* Source all of these files if they exist:

		** ~/.bash/.bash_path
		** ~/.bash/.bash_prompt
		** ~/.bash/.bash_exports
		** ~/.bash/.bash_aliases
		** ~/.bash/.bash_functions
		** ~/.bash_extra.bash

	* Setup some history stuff and bash-completion
